# Base URL of the deployed CodeArena API, e.g. https://api.codearena.example.com
# Leave unset for local development — the Vite dev server proxies /api to
# http://localhost:5000 automatically (see vite.config.js).
VITE_API_URL=
